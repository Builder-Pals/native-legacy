return {
    --- Protects an instance from being destroyed when a new game is loaded.
    RELOAD_PROTECTION = "nl_reload_protection",

    --- Causes the script environment variable to be set its parent
    POINTER_SCRIPT = "nlp"
}